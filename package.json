{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "pretest": "node scripts/check-boundaries.mjs && node scripts/check-theme.mjs",
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
